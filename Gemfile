# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# gem "rails"

gem "jekyll", "~> 4.1"
gem "addressable", ">= 2.8.0" #added per github suugestion
# added later might need to remove...for scholar cite
group :jekyll_plugins do
    #gem "jekyll-feed"
    #gem "jekyll-seo-tag"
    gem "jekyll-scholar"
end